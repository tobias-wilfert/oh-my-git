name: "Build"

on:
  push:
    tags:
      - "*"
permissions:
  contents: write

env:
  PROJECT_NAME: oh-my-git
  GODOT_VERSION: 3.6.2

jobs:
  build:
    runs-on: ubuntu-22.04
    container:
      image: barichello/godot-ci@sha256:026e1f652dd1d718073bb5c32372b3a5d73e7ad7bfe13b9052423604fca6918f
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: apt-get update && apt-get install -y rsync p7zip make
      - name: Setup
        run: |
          mkdir -p ~/.local/share/godot/templates
          mv /root/.local/share/godot/export_templates/$GODOT_VERSION.stable ~/.local/share/godot/templates/$GODOT_VERSION.stable
      - name: Build Linux
        run: make linux
      - name: Build macOS
        run: make macos
      - name: Build Windows
        run: make windows
      - name: Clean up
        run: make clean-unzipped
      - name: Publish to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/${{ env.PROJECT_NAME }}-linux.zip
            build/${{ env.PROJECT_NAME }}-macos.zip
            build/${{ env.PROJECT_NAME }}-windows.zip
      - name: Deploy Linux
        uses: josephbmanley/butler-publish-itchio-action@v1.0.3
        env:
          BUTLER_CREDENTIALS: ${{ secrets.BUTLER_CREDENTIALS }}
          CHANNEL: linux
          ITCH_GAME: ${{ env.PROJECT_NAME }}
          ITCH_USER: blinry
          PACKAGE: build/${{ env.PROJECT_NAME }}-linux.zip
          VERSION: ${{ steps.tag_name.outputs.TAG_NAME }}
      - name: Deploy macOS
        uses: josephbmanley/butler-publish-itchio-action@v1.0.3
        env:
          BUTLER_CREDENTIALS: ${{ secrets.BUTLER_CREDENTIALS }}
          CHANNEL: osx
          ITCH_GAME: ${{ env.PROJECT_NAME }}
          ITCH_USER: blinry
          PACKAGE: build/${{ env.PROJECT_NAME }}-macos.zip
          VERSION: ${{ steps.tag_name.outputs.TAG_NAME }}
      - name: Deploy Windows
        uses: josephbmanley/butler-publish-itchio-action@v1.0.3
        env:
          BUTLER_CREDENTIALS: ${{ secrets.BUTLER_CREDENTIALS }}
          CHANNEL: windows
          ITCH_GAME: ${{ env.PROJECT_NAME }}
          ITCH_USER: blinry
          PACKAGE: build/${{ env.PROJECT_NAME }}-windows.zip
          VERSION: ${{ steps.tag_name.outputs.TAG_NAME }}
